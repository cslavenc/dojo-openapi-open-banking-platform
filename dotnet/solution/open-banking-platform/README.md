# Solution

If using `openapi-generator-cli`:

- Move to `.openapi`
- execute `npx @openapitools/openapi-generator-cli generate -i openapi.yaml -g csharp -o ./gen`
- the generated files are found in `.openapi/gen`
